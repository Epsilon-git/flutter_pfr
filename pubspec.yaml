name: pfr
description: ПФР - Обращения

version: 1.0.0+1

environment:
  sdk: ">=3.0.6 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
    
  flutter_web_plugins:
    sdk: flutter
  #
  cupertino_icons: ^1.0.2
  #
  flex_color_scheme: ^6.1.2
  get: ^4.6.5
  flutter_dropzone: ^3.0.6
  dotted_border: ^2.0.0+3
  realm: ^1.4.0
  firebase_core: ^2.15.1
  firebase_auth: ^4.9.0
  cloud_firestore: ^4.9.1
  intl: ^0.18.1
  shared_preferences: ^2.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  #
  lint: ^2.1.2

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
